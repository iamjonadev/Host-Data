name: Deploy to AdoteamAB Cloud

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Deploy to server
        run: |
          echo "🚀 Deploying to host.adoteam.dev..."
          cd /home/iamjonadev/AdoteamAB
          sudo docker compose build client_host.adoteam.dev
          sudo docker compose up -d client_host.adoteam.dev
          echo "✅ Deployment completed!"
